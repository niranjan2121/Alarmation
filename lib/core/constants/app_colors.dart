import 'package:flutter/material.dart';

class AppColors {
  // The absolute core canvas background (Soft Off-White)
  static const Color neumorphicBackground = Color(0xFFF1F3F6);

  // Highlight shadow (Top-Left)
  static const Color lightShadow = Color(0xFFFFFFFF);

  // Depth shadow (Bottom-Right) - Softer, less saturated grey
  static const Color darkShadow = Color(0xFFD1D9E6);

  // The Reddish-Orange Glowing Accent
  static const Color accentOrange =
      Color(0xFFFF453A); // Apple's vibrant reddish-orange

  // Pure Black for heavy contrast UI elements and text
  static const Color pureBlack = Color(0xFF000000);
  static const Color textDark = Color(0xFF1C1C1E);
  static const Color textMuted = Color(0xFF8E8E93);
}
